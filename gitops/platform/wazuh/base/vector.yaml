# Placeholder (Vector runs elsewhere in your stack; keeping generator for consistency)
sources:
  dummy:
    type: stdin
sinks:
  console:
    type: console
    inputs: ["dummy"]
    encoding:
      codec: json
