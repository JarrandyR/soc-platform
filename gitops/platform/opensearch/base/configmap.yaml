apiVersion: v1
kind: ConfigMap
metadata:
  name: opensearch-config
data:
  opensearch.yml: |
    cluster.name: soc-opensearch
    discovery.type: single-node
    network.host: 0.0.0.0

    plugins.security.disabled: true