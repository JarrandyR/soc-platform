apiVersion: v1
kind: Secret
metadata:
  name: opensearch-admin-credentials
  namespace: platform-opensearch
  labels:
    app.kubernetes.io/name: opensearch
    app.kubernetes.io/component: snapshots
type: Opaque
# NOTE: This file is GitOps-managed, but we are NOT embedding your password in Git.
# You already created the Secret in-cluster. This manifest exists so Kustomize has a resource,
# but does not overwrite your live Secret (no stringData/data included).